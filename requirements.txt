aiohttp>=3.9
beautifulsoup4>=4.12
# Interfaccia grafica (app locale nel browser)
flask>=3.0
# Render JavaScript (browser headless) per le email caricate dinamicamente
playwright>=1.40
# OCR per leggere le email messe come immagini
pytesseract>=0.3.10
Pillow>=10.0
